# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

本项目的开发规则（环境与命令、模块结构、数据坑点、commit 规范）维护在 `AGENTS.md`，由 codex 与 Claude Code 共用，不再重复维护两份。Claude Code 加载本文件时会通过下面的引用自动导入其完整内容：

@AGENTS.md